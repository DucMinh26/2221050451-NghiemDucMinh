@model IEnumerable<BT_NET.Models.Entities.ExportTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ExportDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExportDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ExportTicketID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ExportTicketID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ExportTicketID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
